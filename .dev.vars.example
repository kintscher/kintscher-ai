# Local dev secrets for `wrangler dev` / `astro dev`. Copy to `.dev.vars` and fill in.
# Production: set via `wrangler secret put ANTHROPIC_API_KEY`.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
